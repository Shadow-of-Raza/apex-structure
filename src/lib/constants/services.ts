// src/lib/constants/services.ts
import { Service } from '@/lib/types/service'

/**
 * @deprecated Use getAllServices() from @/lib/utils/services instead for dynamic data.
 * This array is now empty as data is managed through the CMS.
 */
export const servicesData: Service[] = []